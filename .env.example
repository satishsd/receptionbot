# Server
PORT=3000
NODE_ENV=production

# Database (CloudPanel PostgreSQL)
# Find credentials: CloudPanel -> Databases -> your database -> Show Credentials
# Format: ******127.0.0.1:5432/DB_NAME?schema=public
DATABASE_URL="******127.0.0.1:5432/receptionbot?schema=public"

# App
APP_NAME="ReceptionBot"
APP_VERSION="1.0.0"

# Admin Panel
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme123
ADMIN_SECRET=replace-with-a-long-random-secret

# WhatsApp Cloud API (Meta)
WHATSAPP_VERIFY_TOKEN=your-webhook-verify-token
WHATSAPP_ACCESS_TOKEN=your-whatsapp-access-token
WHATSAPP_PHONE_NUMBER_ID=your-phone-number-id

# Cron job retention settings (optional, defaults shown)
SESSION_RETENTION_DAYS=30
WEBHOOK_RETENTION_DAYS=7
