# Tests

Unit and integration tests will be added in Phase 5 (production hardening).

## Planned test coverage

- `bot/intents/recognizer.test.ts` — intent recognition accuracy
- `bot/handlers/*.test.ts` — individual handler logic
- `bot/engine.test.ts` — end-to-end conversation flows
- `channels/web/chatRoutes.test.ts` — API endpoint integration tests
- `db/repositories/*.test.ts` — repository layer with test database
